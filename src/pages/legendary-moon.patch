--- /mnt/user-data/uploads/CryptoPackOpener.jsx	2026-05-09 00:17:19.644217000 +0000
+++ CryptoPackOpener.jsx	2026-05-09 00:20:00.033234209 +0000
@@ -119,6 +119,65 @@
     80% { transform: translate(2px,-4px); }
   }
 
+  /* ── LEGENDARY MOON ANIMATIONS ── */
+  @keyframes leg-yellow-flash {
+    0%   { opacity: 0; }
+    15%  { opacity: 1; }
+    55%  { opacity: 1; }
+    100% { opacity: 0; }
+  }
+  @keyframes leg-moon-in {
+    0%   { opacity: 0; }
+    100% { opacity: 1; }
+  }
+  @keyframes leg-star-trail {
+    0%   { transform: translate(-50%,-50%) translateY(-160vh) rotate(-35deg) scale(1.4); opacity: 0; filter: blur(6px); }
+    18%  { opacity: 1; filter: blur(2px); }
+    55%  { transform: translate(-50%,-50%) translateY(28px) rotate(-4deg) scale(1.08); opacity: 1; filter: blur(0px); }
+    68%  { transform: translate(-50%,-50%) translateY(-60px) rotate(1deg) scale(0.93); }
+    78%  { transform: translate(-50%,-50%) translateY(16px) rotate(-0.5deg) scale(1.04); }
+    88%  { transform: translate(-50%,-50%) translateY(-10px) rotate(0.2deg) scale(0.99); }
+    100% { transform: translate(-50%,-50%) translateY(0px) rotate(0deg) scale(1); opacity: 1; filter: blur(0px); }
+  }
+  @keyframes leg-glow-trail {
+    0%   { opacity: 0; transform: translate(-50%,-50%) translateY(-160vh) rotate(-35deg) scaleX(0.5); }
+    18%  { opacity: 0.9; }
+    55%  { opacity: 0; transform: translate(-50%,-50%) translateY(28px) rotate(-4deg) scaleX(1); }
+    100% { opacity: 0; }
+  }
+  @keyframes leg-impact-shockwave {
+    0%   { transform: translate(-50%,-50%) scale(0.1); opacity: 1; }
+    100% { transform: translate(-50%,-50%) scale(6); opacity: 0; }
+  }
+  @keyframes leg-impact-flash {
+    0%   { opacity: 0; }
+    5%   { opacity: 0.9; }
+    40%  { opacity: 0; }
+    100% { opacity: 0; }
+  }
+  @keyframes leg-dust-ring {
+    0%   { transform: translate(-50%,-50%) scale(0.2) scaleY(0.25); opacity: 0.85; }
+    100% { transform: translate(-50%,-50%) scale(4) scaleY(0.25); opacity: 0; }
+  }
+  @keyframes leg-card-glow-pulse {
+    0%,100% { box-shadow: 0 0 30px 8px rgba(255,217,107,0.55), 0 0 80px 20px rgba(200,160,0,0.3); }
+    50%     { box-shadow: 0 0 70px 22px rgba(255,217,107,0.95), 0 0 160px 50px rgba(220,180,0,0.65); }
+  }
+  @keyframes leg-float-idle {
+    0%   { transform: translateY(0px) rotate(0deg); }
+    30%  { transform: translateY(-10px) rotate(-1.2deg); }
+    60%  { transform: translateY(-4px) rotate(0.8deg); }
+    100% { transform: translateY(0px) rotate(0deg); }
+  }
+  @keyframes leg-btn-pulse {
+    0%,100% { box-shadow: 0 0 12px rgba(255,217,107,0.4); }
+    50%     { box-shadow: 0 0 30px rgba(255,217,107,0.95), 0 0 56px rgba(200,160,0,0.55); }
+  }
+  @keyframes leg-stars-twinkle {
+    0%,100% { opacity: 0.5; }
+    50%     { opacity: 1; }
+  }
+
   /* ── MYTHIC ANIMATIONS ── */
   @keyframes cpo-mythic-blackflash {
     0%   { opacity: 0; }
@@ -600,6 +659,317 @@
   );
 }
 
+// ─── Legendary Reveal Sequence ───────────────────────────────
+// Timeline:
+//   0–900ms   → 3× yellow screen flashes
+//   900ms     → moon scene fades in
+//   1300ms    → card shoots in like a shooting star with yellow aura trail
+//   2200ms    → impact: shockwave + dust ring + flash
+//   2350ms    → card settles (low-gravity idle float)
+//   2600ms    → "Reveal Card" button appears
+function LegendaryRevealSequence({ card, onClose }) {
+  const meta = RARITY_META["legendary"];
+
+  const [flashCount, setFlashCount]   = useState(0); // 0=none,1,2,3
+  const [showMoon,   setShowMoon]     = useState(false);
+  const [showCard,   setShowCard]     = useState(false);
+  const [crashed,    setCrashed]      = useState(false);
+  const [showImpact, setShowImpact]   = useState(false);
+  const [floating,   setFloating]     = useState(false);
+  const [showReveal, setShowReveal]   = useState(false);
+  const [flipped,    setFlipped]      = useState(false);
+  const [showContinue, setShowContinue] = useState(false);
+
+  useEffect(() => {
+    const timers = [];
+    const t = (fn, ms) => { const id = setTimeout(fn, ms); timers.push(id); };
+
+    // 3 yellow flashes
+    t(() => setFlashCount(1), 0);
+    t(() => setFlashCount(0), 220);
+    t(() => setFlashCount(2), 380);
+    t(() => setFlashCount(0), 600);
+    t(() => setFlashCount(3), 750);
+    t(() => setFlashCount(0), 970);
+
+    // Moon scene
+    t(() => setShowMoon(true), 1050);
+
+    // Shooting star card
+    t(() => setShowCard(true), 1350);
+
+    // Impact effects
+    t(() => { setShowImpact(true); setTimeout(() => setShowImpact(false), 900); }, 2200);
+    t(() => { setCrashed(true); }, 2280);
+
+    // Low-gravity float
+    t(() => setFloating(true), 2450);
+
+    // Reveal button
+    t(() => setShowReveal(true), 2700);
+
+    return () => timers.forEach(clearTimeout);
+  }, []);
+
+  const handleReveal = () => {
+    setFlipped(true);
+    setFloating(false);
+    setTimeout(() => setShowContinue(true), 900);
+  };
+
+  // Moon stars (static seed so they don't re-render)
+  const stars = useMemo(() => Array.from({ length: 80 }, (_, i) => ({
+    x: ((i * 137.5) % 100),
+    y: ((i * 97.3) % 70),
+    size: 1 + (i % 3),
+    delay: (i * 0.07) % 3,
+    dur: 1.8 + (i % 5) * 0.4,
+  })), []);
+
+  return (
+    <div style={{
+      position: "fixed", inset: 0, zIndex: 9999,
+      display: "flex", alignItems: "center", justifyContent: "center",
+      background: "#000", overflow: "hidden",
+    }}>
+
+      {/* Yellow screen flashes */}
+      {flashCount > 0 && (
+        <div style={{
+          position: "absolute", inset: 0, zIndex: 60, pointerEvents: "none",
+          background: "radial-gradient(ellipse at center, rgba(255,230,80,0.98) 0%, rgba(255,200,0,0.85) 55%, rgba(180,130,0,0.5) 100%)",
+          boxShadow: "inset 0 0 120px rgba(255,255,100,0.6)",
+          animation: "leg-yellow-flash 0.22s ease-out both",
+        }} />
+      )}
+
+      {/* Moon scene background */}
+      {showMoon && (
+        <>
+          {/* Deep space */}
+          <div style={{
+            position: "absolute", inset: 0,
+            background: "radial-gradient(ellipse at 50% 30%, #0d0a20 0%, #050510 60%, #000 100%)",
+            animation: "leg-moon-in 0.5s ease-out both",
+          }} />
+
+          {/* Stars */}
+          <div style={{ position: "absolute", inset: 0, pointerEvents: "none", overflow: "hidden" }}>
+            {stars.map((s, i) => (
+              <div key={i} style={{
+                position: "absolute",
+                left: `${s.x}%`, top: `${s.y}%`,
+                width: s.size, height: s.size,
+                borderRadius: "50%",
+                background: "#fff",
+                boxShadow: `0 0 ${s.size * 2}px rgba(255,255,200,0.8)`,
+                animation: `leg-stars-twinkle ${s.dur}s ease-in-out ${s.delay}s infinite`,
+              }} />
+            ))}
+          </div>
+
+          {/* Moon surface at bottom */}
+          <div style={{
+            position: "absolute", bottom: 0, left: 0, right: 0, height: "38%",
+            background: "radial-gradient(ellipse at 50% 120%, #d4c88a 0%, #b8a86a 30%, #8a7a48 60%, #5a5030 100%)",
+            borderRadius: "50% 50% 0 0 / 18% 18% 0 0",
+            boxShadow: "0 -4px 40px rgba(180,160,80,0.3), inset 0 2px 20px rgba(255,230,100,0.15)",
+          }} />
+
+          {/* Moon craters */}
+          {[
+            { left: "12%", bottom: "12%", size: 40 },
+            { left: "30%", bottom: "6%",  size: 22 },
+            { left: "60%", bottom: "14%", size: 55 },
+            { left: "78%", bottom: "8%",  size: 28 },
+            { left: "88%", bottom: "18%", size: 18 },
+          ].map((c, i) => (
+            <div key={i} style={{
+              position: "absolute", left: c.left, bottom: c.bottom,
+              width: c.size, height: c.size * 0.4,
+              borderRadius: "50%",
+              background: "transparent",
+              border: "2px solid rgba(100,90,50,0.55)",
+              boxShadow: `inset 0 3px 8px rgba(0,0,0,0.4), 0 0 6px rgba(255,220,100,0.1)`,
+              animation: `leg-stars-twinkle 3s ease-in-out ${i * 0.6}s infinite`,
+            }} />
+          ))}
+
+          {/* Earth in sky */}
+          <div style={{
+            position: "absolute", top: "8%", right: "10%",
+            width: 64, height: 64, borderRadius: "50%",
+            background: "radial-gradient(ellipse at 35% 30%, #4a9ef8 0%, #1a6abf 45%, #0a3060 80%, #051830 100%)",
+            boxShadow: "0 0 20px rgba(74,158,248,0.4), 0 0 40px rgba(50,100,200,0.2)",
+            overflow: "hidden",
+          }}>
+            <div style={{ position: "absolute", top: "20%", left: "10%", width: "40%", height: "30%", borderRadius: "50%", background: "rgba(80,160,60,0.7)" }} />
+            <div style={{ position: "absolute", top: "50%", left: "30%", width: "55%", height: "28%", borderRadius: "50%", background: "rgba(80,160,60,0.6)" }} />
+            <div style={{ position: "absolute", inset: 0, borderRadius: "50%", background: "linear-gradient(135deg, rgba(255,255,255,0.18) 0%, transparent 60%)" }} />
+          </div>
+
+          {/* Ambient golden light on moon surface */}
+          <div style={{
+            position: "absolute", bottom: "30%", left: "50%",
+            transform: "translateX(-50%)",
+            width: 400, height: 200, borderRadius: "50%",
+            background: "radial-gradient(ellipse at center, rgba(255,200,50,0.18) 0%, transparent 70%)",
+            pointerEvents: "none",
+          }} />
+        </>
+      )}
+
+      {/* Shooting star glow trail (behind card) */}
+      {showCard && !crashed && (
+        <div style={{
+          position: "absolute", left: "50%", top: "48%",
+          width: 340, height: 700,
+          background: "linear-gradient(to bottom, rgba(255,220,60,0.0) 0%, rgba(255,220,60,0.55) 30%, rgba(255,240,120,0.9) 70%, rgba(255,255,200,0.6) 100%)",
+          filter: "blur(12px)",
+          transformOrigin: "center bottom",
+          pointerEvents: "none", zIndex: 8,
+          animation: "leg-glow-trail 0.95s cubic-bezier(0.15,0.85,0.2,1) both",
+        }} />
+      )}
+
+      {/* Impact effects */}
+      {showImpact && (
+        <>
+          <div style={{ position: "absolute", left: "50%", top: "48%", zIndex: 15, width: 280, height: 280, borderRadius: "50%", border: "3px solid rgba(255,220,60,0.95)", animation: "leg-impact-shockwave 0.7s ease-out both", pointerEvents: "none" }} />
+          <div style={{ position: "absolute", left: "50%", top: "48%", zIndex: 15, width: 280, height: 280, borderRadius: "50%", border: "2px solid rgba(255,180,0,0.6)", animation: "leg-impact-shockwave 0.7s ease-out 0.08s both", pointerEvents: "none" }} />
+          <div style={{ position: "absolute", left: "50%", top: "50%", zIndex: 14, width: 360, height: 100, borderRadius: "50%", border: "2px solid rgba(255,220,60,0.7)", animation: "leg-dust-ring 0.65s ease-out both", pointerEvents: "none" }} />
+          <div style={{ position: "absolute", inset: 0, zIndex: 16, pointerEvents: "none", background: "rgba(255,220,80,0.3)", animation: "leg-impact-flash 0.45s ease-out both" }} />
+        </>
+      )}
+
+      {/* The card */}
+      {showCard && (
+        <div style={{ position: "relative", zIndex: 10, display: "flex", flexDirection: "column", alignItems: "center", gap: 28 }}>
+
+          {/* Yellow shooting-star aura (visible during crash) */}
+          {!crashed && (
+            <div style={{
+              position: "absolute", left: "50%", top: "50%",
+              width: 320, height: 450,
+              borderRadius: "50%",
+              background: "radial-gradient(ellipse at center, rgba(255,230,80,0.65) 0%, rgba(255,180,0,0.35) 45%, transparent 72%)",
+              filter: "blur(18px)",
+              transform: "translate(-50%,-50%)",
+              pointerEvents: "none", zIndex: -1,
+            }} />
+          )}
+
+          {/* 3-D flip container */}
+          <div style={{ perspective: 1200 }}>
+            <div style={{
+              position: "relative", width: 288, height: 384,
+              transformStyle: "preserve-3d",
+              transition: flipped ? "transform 0.75s cubic-bezier(0.2,0.9,0.3,1.1)" : "none",
+              transform: flipped ? "rotateY(180deg)" : "rotateY(0deg)",
+              animation: !crashed
+                ? "leg-star-trail 0.95s cubic-bezier(0.15,0.85,0.2,1) both"
+                : floating
+                ? "leg-float-idle 3.6s ease-in-out infinite, leg-card-glow-pulse 2.4s ease-in-out infinite"
+                : "leg-card-glow-pulse 2.4s ease-in-out infinite",
+            }}>
+
+              {/* BACK FACE */}
+              <div style={{
+                position: "absolute", inset: 0, borderRadius: 12,
+                backfaceVisibility: "hidden",
+                border: `2px solid ${meta.color}`,
+                background: "linear-gradient(160deg, #1c1800, #0c0c10)",
+                overflow: "hidden",
+                boxShadow: `0 0 50px ${meta.glow}, inset 0 0 35px ${meta.glow}`,
+              }}>
+                <div style={{ position: "absolute", inset: 0, opacity: 0.15, pointerEvents: "none", zIndex: 3, background: "linear-gradient(135deg, transparent 30%, rgba(255,255,255,0.35) 50%, transparent 70%)" }} />
+                <div style={{ width: "100%", height: "100%", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 10 }}>
+                  <div style={{ position: "absolute", inset: 0, opacity: 0.07, backgroundImage: "linear-gradient(rgba(255,217,107,0.2) 1px, transparent 1px), linear-gradient(90deg, rgba(255,217,107,0.2) 1px, transparent 1px)", backgroundSize: "30px 30px" }} />
+                  <span style={{ fontSize: 80, fontFamily: "Georgia, serif", background: "linear-gradient(135deg, #FFD96B, #7a5e22)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent", position: "relative", zIndex: 1 }}>$</span>
+                  <span style={{ fontSize: 9, textTransform: "uppercase", letterSpacing: "0.4em", color: "#FFD96B88", position: "relative", zIndex: 1 }}>Legendary</span>
+                  <div style={{ position: "absolute", inset: 10, borderRadius: 8, border: "1px solid rgba(255,217,107,0.3)", pointerEvents: "none" }} />
+                </div>
+              </div>
+
+              {/* FRONT FACE */}
+              <div style={{
+                position: "absolute", inset: 0, borderRadius: 12,
+                backfaceVisibility: "hidden",
+                transform: "rotateY(180deg)",
+                border: `2px solid ${meta.color}`,
+                background: "linear-gradient(160deg, #1c1800, #0c0c10)",
+                overflow: "hidden",
+                boxShadow: `0 0 60px ${meta.glow}, inset 0 0 40px ${meta.glow}`,
+              }}>
+                <div style={{ position: "absolute", inset: 0, opacity: 0.15, pointerEvents: "none", zIndex: 3, background: "linear-gradient(135deg, transparent 30%, rgba(255,255,255,0.35) 50%, transparent 70%)" }} />
+                <div style={{ position: "relative", zIndex: 4, display: "flex", justifyContent: "space-between", alignItems: "center", padding: "8px 12px 6px", background: "rgba(0,0,0,0.6)", borderBottom: `1px solid ${meta.color}55` }}>
+                  <span style={{ fontSize: 10, fontWeight: 800, textTransform: "uppercase", letterSpacing: "0.3em", color: meta.color }}>Legendary</span>
+                  <span style={{ fontSize: 10, fontWeight: 700, color: "#666", textTransform: "uppercase", letterSpacing: "0.15em" }}>{card.series}</span>
+                </div>
+                <div style={{ flex: 1, position: "relative", overflow: "hidden", height: 280 }}>
+                  <img src={card.image} alt={card.name}
+                    style={{ width: "100%", height: "100%", objectFit: "cover", objectPosition: "center", display: "block" }}
+                    onError={e => { e.currentTarget.style.display = "none"; e.currentTarget.parentElement.style.background = `linear-gradient(160deg, ${meta.color}22, #0c0c10)`; }}
+                  />
+                  <div style={{ position: "absolute", bottom: 0, left: 0, right: 0, height: "45%", background: "linear-gradient(to top, rgba(10,8,0,0.97), transparent)", pointerEvents: "none" }} />
+                </div>
+                <div style={{ position: "relative", zIndex: 4, padding: "10px 14px 12px", background: "rgba(0,0,0,0.75)", borderTop: `1px solid ${meta.color}44` }}>
+                  <div style={{ fontFamily: "Georgia, serif", fontSize: 17, fontWeight: 700, color: "#f0ece0", marginBottom: 5 }}>{card.name}</div>
+                  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
+                    <span style={{ fontSize: 13, fontWeight: 800, color: meta.color, letterSpacing: "0.08em", textTransform: "uppercase" }}>OVR {card.ovr}</span>
+                    <span style={{ fontSize: 11, color: "#555", fontStyle: "italic" }}>{card.series}</span>
+                  </div>
+                </div>
+              </div>
+
+            </div>
+          </div>
+
+          {/* Reveal button */}
+          {showReveal && !flipped && (
+            <button
+              className="cpo-float-up"
+              onClick={handleReveal}
+              style={{
+                background: "linear-gradient(135deg, #c9a84c, #7a5e22)",
+                border: "1px solid #FFD96B",
+                color: "#0c0c10",
+                fontWeight: 700, fontSize: 15, padding: "13px 38px", borderRadius: 8,
+                cursor: "pointer", letterSpacing: "0.15em", textTransform: "uppercase",
+                animation: "leg-btn-pulse 1.8s ease-in-out infinite",
+                transition: "transform 0.15s",
+              }}
+              onMouseEnter={e => e.currentTarget.style.transform = "scale(1.05)"}
+              onMouseLeave={e => e.currentTarget.style.transform = "scale(1)"}
+            >
+              ✦ Reveal Card
+            </button>
+          )}
+
+          {/* Continue button */}
+          {showContinue && (
+            <button
+              className="cpo-float-up"
+              onClick={onClose}
+              style={{
+                background: "transparent", border: "1px solid #FFD96B", color: "#FFD96B",
+                fontWeight: 700, fontSize: 14, padding: "12px 40px", borderRadius: 8,
+                cursor: "pointer", letterSpacing: "0.2em", textTransform: "uppercase",
+                transition: "all 0.2s", boxShadow: "0 0 12px rgba(255,217,107,0.3)",
+                animationDelay: "0.5s",
+              }}
+              onMouseEnter={e => { e.currentTarget.style.background = "rgba(255,217,107,0.12)"; e.currentTarget.style.boxShadow = "0 0 24px rgba(255,217,107,0.6)"; }}
+              onMouseLeave={e => { e.currentTarget.style.background = "transparent"; e.currentTarget.style.boxShadow = "0 0 12px rgba(255,217,107,0.3)"; }}
+            >
+              Continue Trading
+            </button>
+          )}
+        </div>
+      )}
+    </div>
+  );
+}
+
 // ─── Epic Reveal Sequence (NEW) ───────────────────────────────
 // Timeline:
 //   0ms     → card crashes from top
@@ -1058,12 +1428,17 @@
     return <MythicRevealSequence card={card} onClose={onClose} />;
   }
 
+  // Legendary gets the new moon shooting-star sequence
+  if (tier === "legendary") {
+    return <LegendaryRevealSequence card={card} onClose={onClose} />;
+  }
+
   // Epic gets the new rave crash sequence
   if (tier === "epic") {
     return <EpicRevealSequence card={card} onClose={onClose} />;
   }
 
-  const isFancy = tier === "legendary";
+  const isFancy = false; // legendary no longer reaches here
   const [flipped, setFlipped] = useState(!isFancy);
   const [introDone, setIntroDone] = useState(!isFancy);
 
