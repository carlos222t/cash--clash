--- a/src/components/social/GodlyProfile.jsx
+++ b/src/components/social/GodlyProfile.jsx
@@ -1,35 +1,23 @@
 import React from 'react';
 import { motion } from 'framer-motion';
 
-// Sparkle particle positions
 const SPARKLES = [
-  { x: '10%',  y: '15%', delay: 0,    size: 14 },
-  { x: '85%',  y: '10%', delay: 0.3,  size: 10 },
-  { x: '92%',  y: '70%', delay: 0.6,  size: 16 },
-  { x: '5%',   y: '75%', delay: 0.9,  size: 12 },
-  { x: '50%',  y: '5%',  delay: 0.2,  size: 8  },
-  { x: '70%',  y: '88%', delay: 0.7,  size: 14 },
-  { x: '25%',  y: '90%', delay: 1.1,  size: 10 },
-  { x: '78%',  y: '40%', delay: 0.4,  size: 8  },
-  { x: '15%',  y: '50%', delay: 0.8,  size: 12 },
-  { x: '60%',  y: '95%', delay: 0.15, size: 10 },
+  { x: '-18%', y: '-18%', delay: 0,    size: 13 },
+  { x: '108%', y: '-14%', delay: 0.35, size: 10 },
+  { x: '112%', y: '68%',  delay: 0.65, size: 15 },
+  { x: '-20%', y: '72%',  delay: 0.95, size: 11 },
+  { x: '46%',  y: '-22%', delay: 0.18, size: 8  },
+  { x: '68%',  y: '108%', delay: 0.72, size: 13 },
+  { x: '22%',  y: '110%', delay: 1.1,  size: 9  },
+  { x: '80%',  y: '36%',  delay: 0.42, size: 8  },
 ];
 
 function Sparkle({ x, y, delay, size }) {
   return (
     <motion.div
       style={{ position: 'absolute', left: x, top: y, pointerEvents: 'none', zIndex: 20 }}
-      animate={{
-        opacity: [0, 1, 0],
-        scale:   [0.5, 1.2, 0.5],
-        rotate:  [0, 180, 360],
-      }}
-      transition={{
-        duration: 1.8,
-        repeat: Infinity,
-        delay,
-        ease: 'easeInOut',
-      }}
+      animate={{ opacity: [0, 1, 0], scale: [0.4, 1.2, 0.4], rotate: [0, 180, 360] }}
+      transition={{ duration: 1.9, repeat: Infinity, delay, ease: 'easeInOut' }}
     >
       <svg width={size} height={size} viewBox="0 0 24 24" fill="none">
         <path d="M12 2 L13.5 10.5 L22 12 L13.5 13.5 L12 22 L10.5 13.5 L2 12 L10.5 10.5 Z"
@@ -39,55 +27,38 @@
   );
 }
 
-/**
- * GodlyWrapper — wrap any avatar/card content with this to add the gold glow + sparkles.
- * Usage:
- *   <GodlyWrapper active={isGodly(profile)}>
- *     <YourAvatarOrCard />
- *   </GodlyWrapper>
- */
 export default function GodlyWrapper({ active, children, className = '' }) {
   if (!active) return <div className={className}>{children}</div>;
 
   return (
     <div className={`relative ${className}`} style={{ isolation: 'isolate' }}>
-      {/* Gold shimmer border */}
+      {/* Animated glow ring — pure box-shadow, no overlay on avatar */}
       <motion.div
-        className="absolute inset-0 rounded-2xl pointer-events-none z-10"
+        className="absolute pointer-events-none"
         style={{
-          background: 'linear-gradient(135deg, #FFD700, #FFA500, #FFD700, #FFEC6E, #FFA500)',
-          backgroundSize: '300% 300%',
-          opacity: 0.55,
-          padding: '3px',
+          inset: -3,
           borderRadius: 'inherit',
+          zIndex: 0,
         }}
-        animate={{ backgroundPosition: ['0% 50%', '100% 50%', '0% 50%'] }}
-        transition={{ duration: 3, repeat: Infinity, ease: 'linear' }}
-      />
-
-      {/* Outer gold glow */}
-      <motion.div
-        className="absolute inset-0 rounded-2xl pointer-events-none z-0"
-        animate={{ boxShadow: [
-          '0 0 16px 4px rgba(255,215,0,0.45), 0 0 32px 8px rgba(255,165,0,0.25)',
-          '0 0 24px 8px rgba(255,215,0,0.65), 0 0 48px 16px rgba(255,165,0,0.35)',
-          '0 0 16px 4px rgba(255,215,0,0.45), 0 0 32px 8px rgba(255,165,0,0.25)',
-        ]}}
-        transition={{ duration: 2, repeat: Infinity, ease: 'easeInOut' }}
+        animate={{
+          boxShadow: [
+            '0 0 0 2.5px #FFD700, 0 0 14px 4px rgba(255,215,0,0.5), 0 0 32px 8px rgba(255,165,0,0.25)',
+            '0 0 0 2.5px #FFA500, 0 0 22px 8px rgba(255,215,0,0.75), 0 0 52px 18px rgba(255,165,0,0.4)',
+            '0 0 0 2.5px #FFEC6E, 0 0 14px 4px rgba(255,215,0,0.5), 0 0 32px 8px rgba(255,165,0,0.25)',
+          ],
+        }}
+        transition={{ duration: 2.2, repeat: Infinity, ease: 'easeInOut' }}
       />
 
-      {/* Sparkles */}
+      {/* Sparkles outside the avatar */}
       {SPARKLES.map((s, i) => <Sparkle key={i} {...s} />)}
 
-      {/* Content sits on top */}
-      <div className="relative z-10">{children}</div>
+      {/* Content — z-index keeps avatar fully visible on top */}
+      <div className="relative" style={{ zIndex: 10 }}>{children}</div>
     </div>
   );
 }
 
-/**
- * GodlyBadgePill — small inline badge shown next to names
- */
 export function GodlyBadgePill() {
   return (
     <motion.span
